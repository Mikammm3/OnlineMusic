<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mika.music.mapper.LoveMusicMapper">

    <delete id="deleteSelLoved">
        delete from love_music
        where music_id in
        <foreach collection="musicIds" separator="," item="id" open="(" close=")">
            #{id}
        </foreach>
    </delete>


</mapper>